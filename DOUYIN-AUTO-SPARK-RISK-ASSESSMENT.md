# 抖音自动续火花风险评估与整改建议

> 文档性质：历史安全审计记录  
> 适用范围：MuzTool 抖音 Cookie 绑定、好友缓存、手动续火花与自动续火花功能  
> 说明：本文记录此前审计发现的问题及建议的整改优先级。后续代码可能已经修复其中部分问题，判断当前实现时应以代码、测试和生产验证结果为准。

## 一、Cookie 安全风险：严重

### 1. Cookie 通过 HTTP 直接传输

此前抖音 Cookie 接口接收原始 Cookie 字符串，没有使用账号密码接口的 RSA 加密信封。

生产入口又是 HTTP，因此网络中间人可能截获 Cookie。Cookie 相当于抖音网页版登录凭证，其泄露风险远高于普通资料泄露。

### 2. Cookie 在用户 JSON 中明文保存

此前只有统一认证密码、邀请码和 FCM Token 使用 AES-GCM 加密。

抖音 Cookie 直接保存在：

```python
user["douyin"]["cookies"]
```

随后整个用户对象被写入 JSON 文件。服务器数据目录一旦泄露，抖音会话可能被直接接管。

### 3. 好友名称、头像和会话 ID 明文缓存

好友名称、头像和会话 ID 不具备直接登录能力，但属于联系人和社交关系隐私数据。应对其进行加密或最小化保存，并严格限制数据文件权限、日志内容和备份范围。

## 二、抖音平台风控风险：高

此前的自动续火花行为具有较明显的自动化特征：

- 从服务器 IP 登录，而不是用户日常手机网络；
- 使用无头 Chromium；
- 每次任务创建全新的浏览器环境；
- 多个目标在短时间内连续发送；
- 多名用户可能使用相同默认文案；
- 每天固定在整点附近执行；
- 页面操作节奏基本固定；
- Cookie 对应的常用设备、地区和服务器运行环境可能不同。

这些情况可能导致：

- 登录状态失效；
- 触发二次安全验证；
- 私信功能暂时受限；
- 消息发送失败或延迟；
- 账号被判定存在异常自动化行为；
- 严重时账号功能受到限制。

即使技术上能够发送，也无法保证该行为符合抖音平台规则，或保证长期运行不会触发限制。

## 三、可能误发给错误会话：高

此前 `muzermat` 账号配置的 5 个目标均没有稳定的会话 ID 和会话类型，因此可能依赖名称匹配。

风险包括：

- 好友改名后无法找到；
- 两个会话名称相同时无法准确选择；
- 单聊搜索结果中存在同名用户时，代码可能使用第一个精确名称结果；
- 页面点击后没有再次核对当前活动会话 ID；
- `CONVERSATION_ACTIVE_SELECTOR` 已定义，但此前没有实际用于发送前确认。

群聊保护相对严格：有明确群聊类型时不会回退到普通搜索。但如果目标类型为空，则不能完全依赖这层保护。

## 四、发送成功判定不够可靠：高

此前的主要成功条件是：

```text
按下 Enter 后，输入框为空
```

输入框清空不一定代表抖音服务器已经接受消息，可能出现：

- 网络失败，但编辑器已经清空；
- 风控提示被忽略；
- 消息仍处于发送中；
- 页面切换到了错误的聊天窗口；
- 消息实际上已经发送成功，但输入框状态读取异常；
- 代码判定失败后在 10 分钟后重试，造成重复发送。

更稳妥的方式是：发送后检查消息是否确实出现在当前会话的消息列表中，并确认页面没有失败、权限限制或风控提示。

## 五、部分成功后不会重试失败目标

此前调度器的逻辑是：只要至少一个目标成功，就写入 `last_auto_run`。

这可能导致：

- 5 个目标中只有 1 个成功；
- 当天仍被视为自动任务已经完成；
- 剩余 4 个失败目标当天不会再自动重试。

这虽然减少了重复发送风险，但也可能导致部分好友当天没有续上火花。

## 六、全部失败时可能反复尝试

如果没有任何目标被判定成功：

- 每 10 分钟允许重新执行；
- 每次重新打开一个无头浏览器；
- 再次依次访问所有目标。

如果失败原因是登录验证、账号风控、页面结构变化或 Cookie 失效，持续重试可能进一步放大异常行为。遇到安全验证、权限限制、结果不明确或 Cookie 失效时，应停止当天自动重试，而不是继续每 10 分钟尝试。

## 七、手动和自动任务可能同时执行

此前没有针对单个用户的续火花执行锁。

如果用户在自动任务执行期间点击“立即执行”，可能同时启动两个 Playwright 浏览器。两个任务使用同一份 Cookie，并可能向相同目标发送消息，存在以下风险：

- 重复发送消息；
- Cookie 会话冲突；
- 产生更明显的异常自动化行为；
- 增加服务器 CPU 和内存占用。

## 八、缺少数量、长度和频率限制

此前后端配置接口没有严格限制：

- 目标最大数量；
- 默认消息长度；
- 自定义消息长度；
- 手动执行频率；
- 好友强制刷新频率。

被误操作或滥用时，可能一次启动大量连续消息发送，增加账号和服务器风险。

## 九、Cookie 不会在每次运行后持久化刷新

导入 Cookie 时会保存浏览器刷新后的 Cookie，但此前正常续火花执行结束后，没有把 `context.cookies()` 写回用户数据。

如果抖音在访问过程中轮换了会话 Cookie：

- 当前任务可能正常完成；
- 新 Cookie 没有保存；
- 下一次任务继续使用旧 Cookie；
- 登录状态可能比预期更快失效。

## 建议的处理优先级

### P0：应尽快处理

1. 抖音 Cookie 改为 AES-GCM 加密保存；
2. Cookie 提交改为 HTTPS 或混合加密传输，不能直接通过 HTTP 提交；
3. 自动执行前要求所有目标都有明确的会话 ID 和会话类型；
4. 发送前再次核对当前活动会话；
5. 发送后确认消息确实出现在当前聊天记录中；
6. 遇到安全验证、Cookie 失效或账号限制时，立即停止当天重试。

### P1：降低误发和重复发送风险

1. 为每个用户增加独立的执行锁；
2. 防止手动任务与自动任务重叠；
3. 设置目标数量和消息长度上限；
4. 对手动运行和好友强制刷新增加频率限制；
5. 将部分失败目标单独记录，不重新发送已经成功的目标；
6. 将“结果不明确”与“明确失败”分开，结果不明确时不要自动重试。

### P2：提高稳定性和可观测性

1. 每次正常执行后加密保存刷新后的 Cookie；
2. 记录每个目标最后一次执行结果和失败原因；
3. 在 WebUI 和 Android 中显示：
   - 最近一次自动尝试时间；
   - 最近成功目标数量；
   - 最近失败目标数量；
   - Cookie 是否失效；
   - 是否遇到安全验证；
4. 当页面结构或 React 内部属性改变时停止发送，而不是退回不可靠的名称匹配。

## 总体结论

此前流程已经具备自动调度、错过时间后补执行、好友缓存、群聊标识、标准/自定义文案、失败重试和线程隔离等能力，但主要短板是：

- Cookie 安全不足；
- 目标身份信息不完整；
- 发送成功确认不足；
- 重试策略过于粗糙；
- 缺少单用户并发执行锁。

抖音自动续火花属于使用登录凭证驱动第三方平台网页操作的自动化功能。即使完成上述技术整改，也只能降低 Cookie 泄露、误发、重复发送和无效重试的风险，不能消除抖音平台自身的风控、账号限制或规则合规风险。正式运行前应采用最小目标数、低频率、人工可随时停用和明确结果审计的策略。

## v1.3.5 整改落实情况

v1.3.5 已按本文 P0、P1、P2 建议完成以下技术整改：

- Cookie 改为 RSA 包装随机 AES-256-GCM 密钥的混合加密信封提交，后端拒绝明文 Cookie 请求；
- Cookie 与好友缓存使用服务端 AES-256-GCM 密文保存，旧明文字段自动迁移并删除；
- 用户 JSON 文件权限收紧为 `0600`，数据子目录权限收紧为 `0700`；
- 重新绑定 Cookie 时自动暂停续火花并清空旧目标，避免不同抖音账号之间复用会话 ID；
- 自动和手动发送均要求稳定会话 ID 与明确会话类型，不再回退到名称搜索；
- 点击目标后再次核对活动会话名称、ID 和类型，无法读取稳定标识时停止发送；
- 发送后必须确认同文案消息在当前页面中的数量增加，输入框清空不再单独视为成功；
- 安全验证、会话失效、页面结构变化、结果不明确和未分类错误会触发安全停止，不在当天自动重试；
- 增加单用户非阻塞执行锁，避免手动和自动任务同时运行；
- 限制目标数量、消息长度、Cookie 数量与字段长度，并限制 Cookie 导入、好友刷新、配置和手动执行频率；
- 自动任务按目标保存当日进度，部分成功后只重试尚未成功且明确可重试的目标；
- 每次浏览器任务结束后加密保存刷新后的 Cookie；
- WebUI 与 Android 同步展示最近尝试、成功/失败/不明确数量、安全暂停原因和单目标最近结果；
- 多目标之间加入随机等待，避免以固定机器速度形成瞬时连续发送；
- 安全整改期间仅 `muzermat` 可使用抖音功能，其他账号的入口被隐藏、API 返回 404、服务端自动开关被强制关闭。

### 仍无法由代码消除的外部风险

上述整改可以降低凭据泄露、误发、重复发送、粗暴重试和固定节奏等风险，但不能消除以下第三方平台风险：

- 服务器出口 IP 与用户日常网络不同；
- Playwright 无头浏览器仍可能被平台识别；
- 自动化私信行为可能不符合抖音当前或未来的平台规则；
- 抖音可随时调整页面结构、登录策略、验证码或私信权限；
- 当前生产入口仍为 HTTP，混合加密不能提供 HTTPS 所具备的服务器身份认证、会话令牌保护与完整链路抗篡改能力。

因此，v1.3.5 的结论是“已完成仓库内可实施的 P0/P1/P2 技术整改”，而不是“抖音账号不会再触发风控”。生产使用仍应保持少量目标、低频率、人工可随时停用，并持续观察实际账号状态。


## v1.3.6 单目标测试发送补充

v1.3.6 增加对单个已配置好友或群聊执行真实测试发送的入口，便于在不触发全部目标的情况下核验 Cookie、会话定位和消息确认流程。该功能不是绕过安全限制的独立发送通道：

- 只能选择已经保存并具有稳定会话 ID、明确会话类型的目标；
- 后端按 `target_key` 在服务端配置中重新查找目标，不接受客户端提交任意名称、文案或会话信息；
- 发送前先保存当前可见的标准/自定义文案，确保测试内容与界面一致；
- 与完整手动执行共享每小时 3 次频率额度；
- 与自动任务和完整手动任务共享同一个单用户执行锁；
- 仍执行活动会话二次核对、消息进入聊天记录确认、Cookie 刷新保存和安全异常停止；
- WebUI 与 Android 都会在真实发送前显示确认提示，并在单目标状态中记录结果。

该入口会实际向第三方平台发送消息，仍然存在平台风控和账号限制风险，应仅用于少量、明确目标的人工验证。
